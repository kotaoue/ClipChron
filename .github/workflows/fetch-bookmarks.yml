name: Fetch Bookmarks

on:
  schedule:
    - cron: '0 0 * * *'   # daily at midnight UTC
  workflow_dispatch:

jobs:
  fetch:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      # TODO (Phase 2): add fetcher steps
      - name: Placeholder
        run: echo "Fetchers will be implemented in Phase 2."
